{
  "name": "landing",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel --open --public-url=/dist",
    "build": "parcel build"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  }
}
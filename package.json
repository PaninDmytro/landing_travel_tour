{
  "name": "landing",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel --open --public-url=/dist",
    "build": "parcel build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.0.0"
  }
}
